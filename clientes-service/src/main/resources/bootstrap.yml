spring:
  application:
    name: clientes-service
  cloud:
    config:
      enabled: true
      uri: http://config-sv:8888
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://db-cliente-service:3306/bazarclienteservice?useSSL=false&createDatabaseIfNotExist=TRUE&serverTimezone=UTC
    username: root
    password: 222987