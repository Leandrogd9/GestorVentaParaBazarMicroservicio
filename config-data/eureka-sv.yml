server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    lease-renewal-interval-in-seconds: 0

admin-paths:
  paths:
    -
      uri: "/clientes/crear"
      method: "POST"
    -
      uri: "/actualizar/{1}[1-9]+[0-9]*"
      method: "PUT"